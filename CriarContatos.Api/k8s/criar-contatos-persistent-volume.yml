apiVersion: v1
kind: PersistentVolume
metadata:
  name: criar-contatos-persistent-volume
  labels:
    app: criar-contatos
    tier: backend
  annotations:
    description: "Volume persistente para dados do microsserviço criar-contatos"
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  storageClassName: default
  hostPath:
    path: /data/criar-contatos/logs
