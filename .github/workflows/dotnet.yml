name: .NET CI Pipeline

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-24.04

    steps:
    - uses: actions/checkout@v4

    - name: Setup .NET
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: 8.0.x

    - name: Restore dependencies
      run: dotnet restore

    - name: Build
      run: dotnet build --no-restore

    - name: Run unit tests
      run: dotnet test --no-build --verbosity normal --logger "trx;LogFileName=FiapGrupo57Fase2.Test/TestResults/test-results.trx"

    - name: Run integration tests
      run: dotnet test --filter Category=Integration --no-build --verbosity normal --logger "trx;LogFileName=FiapGrupo57Fase2.Test/TestResults/integration-test-results.trx"

    - name: List Test Results
      run: |
        echo "Verificando conteúdo do diretório TestResults:"
        ls FiapGrupo57Fase2.Test/TestResults  # Lista o conteúdo do diretório de resultados dos testes para verificação

    - name: Publish Test Results
      uses: actions/upload-artifact@v4  # Atualizado para v4
      with:
        name: test-results
        path: FiapGrupo57Fase2.Test/TestResults  # Garanta que este caminho corresponda ao local onde os resultados são salvos

    - name: Setup Prometheus
      run: docker run -d --name prometheus -p 9090:9090 prom/prometheus

    - name: Setup Grafana
      run: docker run -d --name grafana -p 3000:3000 grafana/grafana

    - name: Wait for Grafana to start 
      run: sleep 30  # Aguarde 30 segundos para permitir que o Grafana inicialize

    - name: Verify Prometheus 
      run: curl -f http://localhost:9090/metrics || exit 1  # Verifica se o Prometheus está acessível
      
    - name: Verify Grafana
      run: curl -L -f http://localhost:3000 || exit 1  # Verifica se o Grafana está acessível e -L segue redirecionamentos

    - name: Grafana Logs 
      if: failure()
      run: docker logs grafana
